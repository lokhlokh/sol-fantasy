# SOL 레전드 카드 지정 폴더

완성된 `.sollegend.json` 패키지를 이 폴더에 넣으세요.

```text
legend-packages/
  LG-김재박-....sollegend.json
  KT-유한준-....sollegend.json
```

`pnpm dev` 또는 `pnpm build`를 실행하면 패키지가 자동으로 인덱싱되고, 카드 목록에서 일괄 표시됩니다. 이 폴더의 파일을 GitHub에 커밋하면 배포 빌드에도 같은 카드가 포함됩니다.